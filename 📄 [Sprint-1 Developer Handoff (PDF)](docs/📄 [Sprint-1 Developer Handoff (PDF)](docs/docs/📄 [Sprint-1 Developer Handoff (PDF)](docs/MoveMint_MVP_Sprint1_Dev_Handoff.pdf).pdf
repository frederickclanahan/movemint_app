# MoveMint — Driver-First MVP (Sprint 1)

📄 [Sprint-1 Developer Handoff (PDF)](docs/MoveMint_MVP_Sprint1_Dev_Handoff.pdf)

## Sprint-1 (MVP) — Scope & Milestones
**Build:**
- Dashboard: today’s shift + history with earnings/hour & miles
- Mileage Tracking: auto GPS, IRS-compliant export (PDF/CSV)
- Expense Tracking: one-tap entry + receipt photo, auto categories, tax-savings estimate
- EV Tools: charge session tracking (kWh/cost), EV vs gas comparison, credit alerts
- Smart Assistant (basic): drive now / wait / change zone

**Tech:** Flutter + Firebase (Auth + Firestore)  
**Deliverable:** TestFlight / Google Play Beta build + walkthrough video

**Milestones / Payments**
1. UI shell & nav  
2. Auth + Shift Logger  
3. Mileage + Expense (+ exports)  
4. EV tools (kWh/cost)  
5. Beta build + handoff

## Run the project
```bash
flutter pub get
# If Firebase is required:
# 1) Add Firebase config files locally (DO NOT commit them)
# 2) Add any --dart-define or .env instructions here
flutter run


### 3. Add Run Instructions for Firebase
Clear instructions help devs avoid confusion. Specify where Firebase config goes (but it’s ignored in `.gitignore`).

### 4. Confirm `.gitignore` Covers Secrets
Review `.gitignore`: ensure `google-services.json`, `GoogleService-Info.plist`, `.env`, and build outputs are excluded.

### 5. Visuals (Optional but Powerful)
Adding one preview screenshot or wireframe in `docs/screens/` and showing it in README would lift the presentation significantly.

---

##  Quick Action Plan

1. **Upload PDF into `docs/`**, not root.
2. **Update README**:
   - Link to the PDF in `docs/`
   - Add the `cd movemint_app` run steps.
3. **Double-check `.gitignore`** includes sensitive files and build artifacts.
4. **(Optional)** Add a preview image under `/docs/screens/` and embed in README.

---

Once these are done, reply with **“Done — audit me”**, and I’ll review the repo again with ruthless clarity to make sure it’s **developer-ready** and compelling.

You're so close—let’s turn this into something that commands action.
::contentReference[oaicite:0]{index=0}

**Note**: Project folder is `movemint_app`, so run:
```bash
cd movemint_app
flutter pub get
flutter run
/lib
  main.dart
  themes.dart
  /screens
    dashboard.dart
    shift_summary.dart
    splash_screen.dart
docs/
  MoveMint_MVP_Sprint1_Dev_Handoff.pdf
  wireframes.pdf
pubspec.yaml
.gitignore
LICENSE


---

 

---

